#accept FLOW_FIELD
if FLOW_FIELD
	return
end

void FieldCheck()
	assert(StateExists("FLOW_FIELD"),"Request to field done, while the field flow has not yet been loaded")
end


global void LoadMap(string Map)
end

global void ChainMap(string Map,string SaskiaLabel)
end

